/** Compatibility re-export — implementation lives in `src/hosts/three`. */
export {
  RustEmbodyHost,
  createRustEmbodyHost,
} from '../../hosts/three/RustEmbodyHost';
export type { RustEmbodyHostConfig } from '../../hosts/three/RustEmbodyHost';
